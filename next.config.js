/** @type {import('next').NextConfig} */

cssConfig = {};
const nextConfig = {
  reactStrictMode: true,
  i18n: {
    locales: ["el", "en"],
    defaultLocale: "el",
  },
}

module.exports = nextConfig
