/** @type {import('next').NextConfig} */

cssConfig = {};
const nextConfig = {
  reactStrictMode: true,
  i18n: {
    locales: ["en", "el"],
    defaultLocale: "en",
  },
}

module.exports = nextConfig
